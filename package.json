{
  "name": "exam-preparation-system",
  "version": "1.0.0",
  "description": "Embedded Systems Specialist Exam Preparation System",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "npm run build:frontend && npm run copy:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "copy:frontend": "cp -r frontend/dist/* . 2>/dev/null || echo 'Copy completed'",
    "build:backend": "cd backend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "lint": "npm run lint:frontend && npm run lint:backend",
    "lint:frontend": "cd frontend && npm run lint",
    "lint:backend": "cd backend && npm run lint",
    "db:generate": "cd database && npx prisma generate",
    "db:push": "cd database && npx prisma db push",
    "db:studio": "cd database && npx prisma studio"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}